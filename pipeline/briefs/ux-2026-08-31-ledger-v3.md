# Brief: ux-2026-08-31-ledger-v3

Goal: rebuild LEDGER as a STANDALONE STUDY at
`ui-demos/ledger-v3/index.html`, to the KP/OS v3 instrument-panel system.
`ui-demos/RULINGS.md` IS the spec; `ui-demos/loadout-eva/` is the reference
implementation; `ui-demos/manual-v3/` (same day, cycle
`ux-2026-08-31-manual-v3`) is the freshest sibling and worked the same
build recipe. The main game code (`kernel-panic-site/`) is OUT OF SCOPE.
The user reviews the study first; integration is a later `/kp-ui
integrate` invocation.

USER DIRECTIVE (2026-08-31, from the fix-selection answer): "MANUAL +
LEDGER v3: finish the law-11 queue: two /kp-ui demo cycles ending at
reviewable demos, as rule 1b prescribes." This brief is the LEDGER half
and CLOSES the law-11 queue when it integrates.

CYCLE SCOPING: `pipeline/BRIEF.md` currently carries the still-open
manual-v3 cycle (its demo is `awaiting`). This brief lives at
`pipeline/briefs/ux-2026-08-31-ledger-v3.md` instead, per the standing
parallel-cycle rule: cycle-scoped briefs, append-only proposal writes,
manifest edits at the last moment.

PROCESS: redesign of an EXISTING window, so per `RULINGS.md` "Process" it
is PURE UI. No loremaster gate, no tutorial gate, no game-code detours.
Gate-shaped concerns go in the demo's `NOTES.md` and one line of report.

## What is being replaced

`windows/ledger.tsx`: the last remaining pre-v3 window besides MANUAL.TXT
(law 11: "small, table-shaped"). Read the shipped component for STRUCTURE
and CONTENT only; its look is the thing being replaced. Read the CURRENT
working tree, not memory. Transcribe every player-facing string verbatim
and every number via the symbol that holds it (law 9 step 3).

## Constraints

- Art budget: none. If the surface has a genuine diegetic-imagery case,
  argue it in a `suggestion` item rather than filing an art order.
- Table-shaped is the hard case here: a ledger's rows must survive the
  height ceiling with NO internal scrollbars (law 8: reflow, page, or
  tab). If rows can outgrow the frame at any run state, reach for the
  measure-then-pack row paginator manual-v3 just proved (its
  `paginateAugments`/SET-pager machinery, generalized from dadlog-v3).
- Law 4 equal-footprint: build at least two run states (sparse early-run,
  dense late-run) and the window height must not move between them.
- Name the VARIATIONS worth reviewing (at minimum SCHEME / CRT / VIEWPORT /
  RUN STATE, plus whatever makes this surface's real decisions visible).
- Check the shipped component for the law-5 img violation pattern flagged
  by `law5-downscale-audit` (a width/height attr pair contradicted by a
  smaller CSS style); report what you find either way.
- Be additive (contract rule 6): at least one `suggestion` item.
- APPEND your items to `pipeline/proposals/ux-agent.json`; never rewrite
  other cycles' items.
